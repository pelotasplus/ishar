# TOOLS.md

Generated by `tools/toolsindex.py`. **Do not edit by hand** — each line is the
first line of that tool's own docstring, so it cannot drift from the tool.

100 tools: 39 standing instruments, 61 task probes.

## Standing instruments

| tool | what it does |
|---|---|
| `tools/addr.py` | Convert between the addresses Spice86 shows and the addresses the listing uses. |
| `tools/anatomy.py` | Byte-by-byte map of one asset, every span labelled with the FORMATS section for it. |
| `tools/bphits.py` | Minimal breakpoint probe: arm one execution breakpoint, count hits, print. |
| `tools/chains.py` | Find ALL sprite chains in an asset, not just the best-scoring one. |
| `tools/checkdocs.py` | Every identifier named in the prose must exist where it is supposed to live. |
| `tools/cover.py` | Coverage of the listing: how much of the code segments decoded as instructions |
| `tools/disasm.sh` | !/usr/bin/env bash |
| `tools/fullscreen.py` | Render an asset's full-screen 320x200 pages. |
| `tools/gdbtrace.py` | Trace the game's DOS file calls: MCP arms the breakpoint, GDB delivers the stops. |
| `tools/goto.py` | Put the party on a given map cell by writing its row/col, then force a redraw. |
| `tools/io.py` | Decode Ishar's .io asset container. |
| `tools/io2png.py` | Render a decoded .io asset to PNG. |
| `tools/ioscan.py` | Extract sprites from a decoded .io asset by walking the sprite chain. |
| `tools/ish` | Drive Ishar under Spice86, deterministically. |
| `tools/mappos.py` | Locate the resident map grid and the party's cell, from scratch. |
| `tools/mclick` | Move the harness mouse to a normalised (x,y) and optionally click. T29c3 made this work. |
| `tools/mouse.py` | Drive Ishar's mouse by writing the variables its INT 33h callback would set. |
| `tools/nudge.py` | Send keys to the running game, from outside the tracer. |
| `tools/onscreen.py` | Inventory: which sprites from which assets are on screen right now. |
| `tools/palcheck.py` | Decide an asset's palette by matching its sprites against a real screenshot. |
| `tools/png.py` | Minimal PNG read/write, so screen work needs no third-party imaging library. |
| `tools/region.py` | Read the party's region id, the authoritative one. |
| `tools/reverify.py` | Re-measure the numbers the documents claim, and compare. |
| `tools/rsp.py` | Minimal GDB remote-serial-protocol client for Spice86's stub. |
| `tools/segmap.py` | Derive the segment map from the unpacked image's relocation table. |
| `tools/symbols.py` | Import the functions Spice86 actually executed into ishar.chani as code seeds. |
| `tools/t08run.sh` | !/bin/bash |
| `tools/timebox` | !/usr/bin/env bash |
| `tools/toolsindex.py` | Generate TOOLS.md from every tool's own docstring. |
| `tools/trace-io.py` | Trace the game's DOS file calls. |
| `tools/unpack.py` | Unpack start.exe. |
| `tools/verify-unpack.py` | Check tools/unpack.py against the emulator, byte for byte. |
| `tools/vmcheck.py` | T38: does the main.io disassembly agree with what the VM actually executes? |
| `tools/vmdis.py` | Disassemble an Ishar script (T30). |
| `tools/vmi.py` | T39: a length-accurate stepper for Ishar's script bytecode. |
| `tools/vmops.py` | Classify the VM's opcode handlers by what they do (T28). |
| `tools/vmseed.py` | Find every jump table in the image and seed its targets as code. |
| `tools/walkto.py` | Walk the party to a map cell, reading its position after every step. |
| `tools/whichsprite.py` | What is drawn at one screen pixel? Search every asset at every palette base. |

## Task probes

Written for one roadmap task, kept because the method is often reusable.

| tool | what it does |
|---|---|
| `tools/t10-capture.py` | Capture the container decoder's input and output side by side (T10). |
| `tools/t11-capture.py` | Capture the emulator's decoded bytes for logo.io -- ground truth for tools/io.py. |
| `tools/t11b-capture.py` | Capture VGA memory and the DAC while the Silmarils logo is up (T11b). |
| `tools/t11e-check.py` | Confirm catalogue ids against live loads: break on load_asset_by_id, log ss:[0b04]. |
| `tools/t11g3-look.py` | Walk a line and capture the viewport at each cell, labelled by the cell's grid value. |
| `tools/t11g3-pos.py` | T11g3: find the party's map position in memory. |
| `tools/t11g3-pos2.py` | Find the party's map coordinates by moving it and diffing DGROUP. |
| `tools/t11g3-probe.py` | Probe which neighbours the party can walk into, and what the grid says about them. |
| `tools/t11g3-watch.py` | Watch a few DGROUP bytes while driving the party. Prints one row per key sent. |
| `tools/t11g3d-look.py` | Capture the viewport along a walk, labelled by the value of the cell AHEAD. |
| `tools/t11g3d-reader.py` | Who reads a map cell, and what do they do with the value? |
| `tools/t11g3d-site.py` | Where in a script's bytecode is the map read? |
| `tools/t11g3d-who.py` | Which script reads the map grid? |
| `tools/t11g3e-region.py` | Find the byte that holds the region id. |
| `tools/t11g3f-lattice.py` | Sample the region caption over a lattice of cells to map the regions. |
| `tools/t11g3f-probe.py` | Read the region caption and the bytes around the party's position record. |
| `tools/t11g3f-writer.py` | Who writes the party's region id? |
| `tools/t11j-probe.py` | Break on DAC writes (port 0x3c9) and report who writes the palette, and from where. |
| `tools/t11k-dir.py` | Find the code that reads an asset's head after decoding -- the directory parser. |
| `tools/t11k-probe.py` | Break where the sprite blitter reads its header and report the source pointer. |
| `tools/t11m-palette.py` | T11m: find how a 4bpp sprite's 0..15 index becomes a 256-colour VGA index. |
| `tools/t11m-palsrc.py` | T11m: find where the 768 palette bytes are read from, by watching them go out. |
| `tools/t11m-sprites.py` | Capture every sprite the game actually draws, straight out of emulator memory. |
| `tools/t11m-verify.py` | T11m acceptance: does the static extraction reproduce the emulator's screen? |
| `tools/t11m2-pair.py` | T11m2 / T11m acceptance: prove `group*16 + nibble` against the framebuffer, and |
| `tools/t11p-diff.py` | T11p: name the viewport's routines by what counts up while walking. |
| `tools/t11p-render.py` | T11p: find the routine that draws the game viewport. |
| `tools/t11p-variant.py` | T11p: does seg_0e97:02dd draw the viewport, and what does it do with size? |
| `tools/t11p-writer.py` | Who writes the viewport? MEMORY_WRITE breakpoint, no keys sent during the probe. |
| `tools/t27-scriptsrc.py` | T27: locate the running script. |
| `tools/t29b-dispatch.py` | T29b: how is vm_statement_table (image 0x0060) indexed? |
| `tools/t29c-action.py` | T29c: which VM primitives does one game action use? |
| `tools/t29c-attribute.py` | T29c: attribute VM primitives to game actions by diffing call counts. |
| `tools/t29c2-mouse.py` | T29c2: find the mouse event handler Ishar installs, then see if it ever fires. |
| `tools/t29d-int33.py` | T29d: does Ishar probe INT 33h at startup? |
| `tools/t36-fbmatch.py` | Does the FORMATS.md-only decoder reproduce what the game actually put on screen? |
| `tools/t36c-writer.py` | Who writes the portrait's pixels? Break on a VRAM write inside buste's sprite. |
| `tools/t37-entry.py` | T37: catch a script running inside an asset, and record where it entered. |
| `tools/t37b-scan.py` | T37b: which assets run script, and where does each one start? |
| `tools/t37c-scan.py` | T37c: the FIRST vm_run entry for each asset that is not main.io. |
| `tools/t37d-switch.py` | T37c/d: catch script SWITCHES instead of statements. |
| `tools/t37f-poll.py` | T37f: entry points by polling DS:SI, not by breaking on vm_run. |
| `tools/t39c-entry.py` | T39c: where does main.io's script actually start? |
| `tools/t40-pos.py` | T40: where does a sprite's screen position come from? |
| `tools/t40b-blit.py` | T40b: catch the in-game sprite blitter and record where it draws. |
| `tools/t42-timeline.py` | Real timings for the boot sequence, without a breakpoint distorting them. |
| `tools/t44-entries.py` | Find a script's entry points statically, for assets never seen executing. |
| `tools/t44-when.py` | Which assets' scripts run during a given window? Poll DS:SI filtered to vm_run. |
| `tools/t53-match.py` | Find where each UI sprite is drawn, by matching its pixels against live video memory. |
| `tools/t53b-chrome.py` | Locate every UI chrome sprite on screen, framebuffer -> file. |
| `tools/t54-rowloop.py` | Read the viewport row loop's registers at the row step. |
| `tools/t54-steps.py` | Tabulate the viewport's two per-row steps against the party's position. |
| `tools/t54b-frames.py` | Capture whole viewport frames, one per step, and report each object's sprite and place. |
| `tools/t54b-objects.py` | Group viewport rows into objects, and name the sprite each one is drawn from. |
| `tools/t54c-instances.py` | Dump the drawable-instance pool: each object's own viewport X and Y. |
| `tools/t54c-order.py` | Is the viewport's draw order repeatable for an unchanged scene? |
| `tools/t54c-slope.py` | How far across the screen does an object move per cell of lateral party movement? |
| `tools/t55-source.py` | Which asset does a viewport object's pixels come from, and which sprite? |
| `tools/t56-writer.py` | Which routine writes a given pixel of the viewport? |
| `tools/t59-globals.py` | Map the VM global variable area by diffing it across one action at a time. |
| `tools/t60-record.py` | Snapshot the VM global area to a named file, and diff two snapshots. |
